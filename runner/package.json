{
  "name": "orchestrator-runner",
  "version": "1.0.0",
  "description": "ACME Orchestrator — Jobs API + server runner (executes Claude Code jobs per project)",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "api": "node src/server.js",
    "runner": "node src/runner-main.js",
    "migrate": "node src/migrate.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@slack/bolt": "^5.0.0",
    "express": "^4.21.2",
    "pg": "^8.13.1",
    "zod": "^3.24.1"
  }
}
